PRC_zhengquan = {
		PRC_16_th_will_come_cin = {  
		icon = decision_CPC         
		available = {                          
			hidden_trigger = {
				always = no
			}
		}
		activation = {
		    always = no                          
			NOT = {
			  has_country_flag = PRC_1th_focus_end
		   }
		}
		cancel_trigger = {
			NOT = {
			   tag = PRC
			}
		hidden_trigger = {
			NOT = {
				has_country_flag = PRC_1th_focus_end
			}
		  }
		}
		is_good = yes
		selectable_mission = no
		days_mission_timeout = 1042         
		timeout_effect = {                 
               country_event = prc_zz.1
			   set_country_flag = PRC_1th_focus_end
       }
  }
    PRC_tenth_rendaw = {  
		icon = decision_PRC_SC         
		available = {                          
			hidden_trigger = {
				always = no
			}
		}
		activation = {
		    always = no                          
			NOT = {
			  has_country_flag = tenth_renda
		   }
		}
		cancel_trigger = {
			NOT = {
			   tag = PRC
			}
			hidden_trigger = {
			NOT = {
				has_country_flag = tenth_renda
			}
		  }
		}
		is_good = yes
		selectable_mission = no
		days_mission_timeout = 1162         
		timeout_effect = {                 
               country_event = prc_zz.4
               set_country_flag = tenth_renda
        }
  }
    
}
